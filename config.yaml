---
# Turn the logging level to debug
debug: true

# Server relative configuration
server:
    # port for running the server
    port: 8080
    
    # interface to run the server
    interface: 0.0.0.0

    # how many threads to run io_service
    numThreads: 1
...
